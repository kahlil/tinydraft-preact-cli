{
  "name": "tinydraft-preact-cli",
  "version": "1.0.0",
  "description": "tinydraft.net ported to Preact with preact-cli.",
  "repository": "kahlil/tinydraft-preact-cli",
  "main": "index.js",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch",
    "test": "eslint src && preact test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^3.19.0",
    "if-env": "^1.0.0",
    "preact-cli": "^1.1.1"
  },
  "dependencies": {
    "@hoodie/client": "^10.1.0",
    "basscss": "^8.0.3",
    "decko": "^1.2.0",
    "localforage": "^1.5.0",
    "normalize.css": "^7.0.0",
    "pouchdb": "^6.2.0",
    "preact": "^8.1.0",
    "preact-compat": "^3.16.0",
    "preact-router": "^2.5.2",
    "snarkdown": "^1.2.2",
    "typescript": "^2.3.4",
    "xstream": "^10.8.0"
  }
}
